name: dimensions_theme
description: Dimensions theme extension
version: 0.0.1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  test: ^1.16.0

flutter: