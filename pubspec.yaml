name: dimensions_theme
description: Define app dimensions such as blank spaces or paddings inside flutter theme.
version: 0.0.4
repository: https://github.com/Pierre2tm/dimensions_theme
issue_tracker: https://github.com/Pierre2tm/dimensions_theme/issues
homepage: https://github.com/Pierre2tm/dimensions_theme
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2
  test: ^1.16.0