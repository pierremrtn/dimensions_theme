name: dimensions_theme
description: Define app dimensions inside flutter theme.
version: 0.1.0
repository: https://github.com/pierremrtn/dimensions_theme
issue_tracker: https://github.com/pierremrtn/dimensions_theme/issues
homepage: https://github.com/pierremrtn/dimensions_theme
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2
  test: ^1.16.0